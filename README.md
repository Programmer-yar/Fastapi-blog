# Blog post

- APIs to create, update & delete user
- APIs to create, update & delete blog posts

## Local Setup

- This project setup is done with `uv` from [astral docs](https://docs.astral.sh/uv/getting-started/installation/)
- Install required packages using:
    `uv sync`
- Run the script using:
    `uv run main.py`
